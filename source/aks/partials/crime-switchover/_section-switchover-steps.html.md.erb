<%
  # Define each environment's steps in the order they should be performed.
  # This is the only place you need to edit to add, remove, or reorder steps.
  # The rendering logic below computes the superset order and data-envs
  # automatically — you never need to write data-envs attributes by hand.
  categories = {
    'DEV' => %w[
      access-dts-services
      update-global-dns
      basic-connectivity
      update-redirect-uris
      update-mi-ado-agent
      update-jenkins-config
      enable-cost-analysis
      lock-inactive-cluster
      destroy-old-cluster
      merge-release-branch
    ],
    'STE' => %w[
      access-dts-services
      update-global-dns
      basic-connectivity
      update-redirect-uris
      update-mi-ado-agent
      enable-cost-analysis
      lock-inactive-cluster
      destroy-old-cluster
      merge-release-branch
    ],
    'SIT' => %w[
      scale-down-source
      access-dts-services
      deploy-workloads
      update-global-dns
      basic-connectivity
      update-redirect-uris
      update-keyvault-networking
      enable-cost-analysis
      lock-inactive-cluster
      destroy-old-cluster
      merge-release-branch
    ],
    'NFT' => %w[
      scale-down-source
      access-dts-services
      deploy-workloads
      update-global-dns
      basic-connectivity
      update-redirect-uris
      update-dynatrace
      update-keyvault-networking
      enable-cost-analysis
      lock-inactive-cluster
      destroy-old-cluster
      merge-release-branch
    ],
    'PRP' => %w[
      request-shuttering
      scale-down-source
      access-dts-services
      deploy-workloads
      deploy-reporting-cronjobs
      update-global-dns
      basic-connectivity
      update-redirect-uris
      update-dynatrace
      update-keyvault-networking
      enable-cost-analysis
      lock-inactive-cluster
      destroy-old-cluster
      merge-release-branch
    ],
    'PRD' => %w[
      request-shuttering
      scale-down-source
      access-dts-services
      deploy-workloads
      deploy-reporting-cronjobs
      update-global-dns
      basic-connectivity
      update-redirect-uris
      update-dynatrace
      update-keyvault-networking
      apply-progression-scaling
      enable-cost-analysis
      lock-inactive-cluster
      destroy-old-cluster
      merge-release-branch
    ],
    'PRX' => %w[
      request-shuttering
      scale-down-source
      access-dts-services
      deploy-workloads
      update-global-dns
      basic-connectivity
      update-redirect-uris
      update-keyvault-networking
      enable-cost-analysis
      lock-inactive-cluster
      destroy-old-cluster
      merge-release-branch
    ]
  }
%>

## Cluster Switchover Steps

<%= partial '/partials/content_filter_steps', locals: {
  category_structure: categories,
  category_order:     'DEV,STE,SIT,NFT,PRP,PRD,PRX',
  partials_directory: 'aks/partials/crime-switchover'
} %>
